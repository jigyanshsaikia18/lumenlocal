# LumenLocal — backend

FastAPI service. See repo-root `README.md` for the full quickstart and `/docs` for specs.

```bash
poetry install
poetry run uvicorn app.main:app --reload
poetry run pytest
```
